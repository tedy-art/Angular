"use strict";
// Define `greetme()` function
function greetme(name) {
    console.log("Hello," + name + "!");
}
;
// Calling `greetme()` Function
let greetings = greetme('Tejas');
// Define `multiply()` Function
let multiply = (x, y) => {
    return x * y;
};
// Calling multiply() function
let multi = multiply(4, 5);
console.log(multi);
